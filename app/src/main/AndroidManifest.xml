<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orwmaprojekt1">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcherr"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherr"
        android:supportsRtl="true"
        android:theme="@style/Theme.Orwmaprojekt1">
        <activity android:name=".pregled"></activity>
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity android:name=".studentirecycler" />
        <activity android:name=".prisutnost" />
        <activity android:name=".pregledstudenata" />
        <activity android:name=".dodajpredmet" />
        <activity android:name=".pocetna" />
        <activity android:name=".login" />
        <activity android:name=".dodavanjestudenta" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>